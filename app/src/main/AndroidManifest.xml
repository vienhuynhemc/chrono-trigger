<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.vientamthuong.chronotrigger">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChronoTrigger"
        tools:ignore="AllowBackup">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".newGame.NewGameActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".presonMap.PresonMapActivity"
            android:screenOrientation="landscape" />
        <activity android:name=".myHome.MyHomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".myHome.MyHomeGroundActivity"
            android:screenOrientation="landscape" />
    </application>

</manifest>